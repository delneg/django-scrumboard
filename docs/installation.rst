============
Installation
============

At the command line::

    $ easy_install django-scrumboard

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-scrumboard
    $ pip install django-scrumboard
